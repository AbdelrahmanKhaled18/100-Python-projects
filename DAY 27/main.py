import tkinter as tk

window = tk.Tk()

window.title("i want to learn python")
window.minsize(400, 400)
my_label = tk.Label(window, text="Python Programming", font=("Arial", 20))
my_label.pack()

window.mainloop()
